# fintech-auth-service
Short: Spring Boot Auth Service for FinTech (JWT, Docker, Kafka-ready).

## Demo / Live
- (optional) Link to deployed service or Docker image

## Features
- User signup / login (JWT)
- Password hashing (BCrypt)
- JWT generation & validation
- Global exception handling & structured error responses
- Logback daily rotating logs
- Docker + docker-compose for local dev
- Ready for Redis, Kafka, Jenkins

## Architecture
- Brief text + embed `docs/architecture.md` diagram/link

## Quick start (dev)
1. Clone
2. Copy `application-example.yml` → `application.yml` and set env vars
3. Run Docker stack:
